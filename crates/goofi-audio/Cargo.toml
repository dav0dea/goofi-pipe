[package]
name = "goofi-audio"
version.workspace = true
edition.workspace = true
license.workspace = true

# Pure, dependency-free real-time audio cores (SampleClock pacing, and — later —
# the AudioRing jitter buffer, continuity/crossfade, and drift correction ported
# from the Python audio/ package). No transport, no cpal here; those compose on top.

[dependencies]
