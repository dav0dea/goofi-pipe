[package]
name = "goofi-node"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
goofi-core = { path = "../goofi-core" }
indexmap = "2"
inventory = "0.3"
# Only `serde_json::from_str` is used (the probe schema's Deserialize impl comes from
# goofi-core); no direct `serde` derive here anymore.
serde_json = "1"
