[package]
name = "goofi-client"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
goofi-core = { path = "../goofi-core" }
serde_json = "1"
# An ARRAY snapshot travels as base64 NPY; the client's render rule decodes it onto stdout.
base64 = "0.22"
